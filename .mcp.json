{
  "mcpServers": {
    "dbt": {
      "command": "bash",
      "args": [
        "-c",
        "exec env DBT_PROJECT_DIR=$(pwd)/dbt_project DBT_PATH=$(pwd)/.venv/bin/dbt uvx dbt-mcp"
      ]
    }
  }
}
