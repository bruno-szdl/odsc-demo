name: "odsc_demo"
version: "1.0.0"
config-version: 2

profile: "odsc_demo"

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  odsc_demo:
    staging:
      +materialized: view
    marts:
      +materialized: table
